<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Thermoliner Seats</title>
	</head>
	<body>
		<slot />
		<script is:inline>
			var h = document.querySelector('header');
			window.onscroll = function() {
				if (window.scrollY > 50) {
					h.style.backgroundColor = 'rgba(0,0,0,0.85)';
					h.style.backdropFilter = 'blur(10px)';
					h.style.paddingTop = '0.8rem';
					h.style.paddingBottom = '0.8rem';
				} else {
					h.style.backgroundColor = 'transparent';
					h.style.backdropFilter = 'none';
					h.style.paddingTop = '1.5rem';
					h.style.paddingBottom = '1.5rem';
				}
			};
		</script>
	</body>
</html>

<style>
	@font-face {
		font-family: 'Chreed';
		src: url('../assets/fonts/ChreedRounded-BoldRounded.ttf') format('truetype');
		font-weight: 700;
		font-style: normal;
		font-display: swap;
	}

	@font-face {
		font-family: 'Just Sans';
		src: url('../assets/fonts/JUST-Sans-Regular.ttf') format('truetype');
		font-weight: 400;
		font-style: normal;
		font-display: swap;
	}

	@font-face {
		font-family: 'Just Sans';
		src: url('../assets/fonts/JUST-Sans-Bold.ttf') format('truetype');
		font-weight: 700;
		font-style: normal;
		font-display: swap;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
		font-family: 'Just Sans', system-ui, -apple-system, sans-serif;
		overflow-x: hidden;
		scroll-behavior: smooth;
	}

	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}

	input,
	textarea,
	select,
	button {
		box-sizing: border-box;
	}
</style>
